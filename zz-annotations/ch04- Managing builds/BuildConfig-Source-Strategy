{
    "kind": "BuildConfig",
    "apiVersion": "v1",
    "metadata": {
        "name": "php-example", 1 - Defines a new BuildConfig named php-example.
        ...output omitted...
    },
    "spec": {
        "triggers": [ 2 - Defines the triggers that start new builds.
            {
                "type": "GitHub",
                "github": {
                    "secret": "gukAWHzq1On4AJlMjvjS"  4 - Authorization string for the webhook, 
                                                        randomly generated by Red Hat OpenShift. 
                                                        External applications send this string as part of 
                                                        the webhook URL to trigger new builds.
                }
            },
            ...output omitted...
        ],
        "runPolicy": "Serial", The runPolicy attribute defines whether a build can start 
                                simultaneously. The Serial value represents that it is not 
                                possible to build simultaneously.
        "source": { 5 - The source attribute is responsible for defining the input 
                        source of the build. In this example, it uses a Git repository.

            "type": "Git",
            "git": {
                "uri": "http://services.lab.example.com/php-helloworld"
            }
        },
        "strategy": { Defines the build strategy to build the final container image. 
                        In this example, it uses the Source strategy.
            "type": "Source",
            "sourceStrategy": {
                "from": {
                    "kind": "ImageStreamTag",
                    "namespace": "openshift",
                    "name": "php:7.0"
                }
            }
        },
        "output": { 7 -The output attribute defines where to push the new container 
                        image after a successful build.
            "to": {
                "kind": "ImageStreamTag",
                "name": "php-example:latest"
            }
        },
        ...output omitted...
    },
    ...output omitted...
}

https://access.redhat.com/documentation/en-us/openshift_container_platform/4.6/html-single/builds/index
https://developers.redhat.com/blog/2018/09/26/source-versus-binary-s2i-workflows-with-red-hat-openshift-application-runtimes